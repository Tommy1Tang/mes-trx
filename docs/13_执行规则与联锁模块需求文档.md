# 执行规则与联锁模块需求文档（L3层）
## 1. 模块概述
负责把L2层的工艺模型转成可执行的控制规则，并管理MBR版本发布、工程变更（ECN）、版本差异对比和运行中安全切换。本模块是"受控执行"的核心保障层。

**架构层：** L3 执行规则、联锁与变更控制层
**涉及表：** pro_execution_rule、pro_interlock_rule、pro_interlock_event、pro_device_command_log、pro_error_proofing_record、pro_mbr_release_record、pro_mbr_change_record、pro_ecn_order、pro_ecn_affected_object、pro_mbr_version_diff

## 2. 需求清单

### 2.1 执行规则

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-001 | 执行规则定义 | P0++ | 定义工序或步骤的防错、校验、放行和阻断规则。规则类型：防错规则（物料/设备/人员校验）、校验规则（参数范围/批次匹配）、放行规则（检验通过/复核确认）、阻断规则（异常/超限/Hold） | 规则定义完整 | 工艺工程师 |
| REQ-RULE-002 | 执行规则关联 | P0++ | 将执行规则关联到MBR步骤或工单工序，支持全局规则和步骤级规则 | 关联关系正确 | 工艺工程师 |
| REQ-RULE-003 | 执行规则优先级 | P0++ | 支持规则优先级配置，当多条规则冲突时按优先级执行 | 优先级逻辑正确 | 工艺工程师 |

### 2.2 联锁规则

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-004 | 联锁规则定义 | P0++ | 定义设备、物料、称量、环境、参数的启动或放行条件。联锁类型：设备联锁（设备状态/校准）、物料联锁（批次/有效期/状态）、称量联锁（允差/复核）、环境联锁（温湿度/压差）、参数联锁（上下限） | 联锁规则定义完整 | 工艺工程师 |
| REQ-RULE-005 | 联锁触发动作 | P0++ | 定义联锁触发后的动作：阻断执行、暂停等待确认、自动生成异常、通知相关人员 | 触发动作配置正确 | 工艺工程师 |
| REQ-RULE-006 | 联锁事件记录 | P0++ | 记录联锁触发、阻断、解除和人工干预过程，包含触发时间、触发原因、处理人、处理结果 | 事件记录完整 | 系统自动 |

### 2.3 防错校验

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-007 | 防错校验记录 | P0++ | 统一记录扫码、视觉、称量、设备状态、物料匹配等防错校验结果。校验类型：扫码防错（物料/设备/人员条码）、视觉防错（条码/标签/外观）、称量防错（目标值/实际值/允差）、设备防错（状态/校准）、物料防错（批次/有效期） | 校验记录完整 | 系统自动 |
| REQ-RULE-008 | 防错不通过处理 | P0++ | 防错校验不通过时的处理流程：阻断执行、触发异常、等待授权人员确认 | 处理流程明确 | 系统自动 |

### 2.4 设备指令

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-009 | 设备指令日志 | P0++ | 记录MES向设备、ThingsBoard、PLC或网关下发的指令，包含指令类型、指令内容、下发时间、执行结果 | 指令日志完整 | 系统自动 |

### 2.5 MBR版本发布

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-010 | MBR发布记录 | P0++ | 记录MBR版本发布、启用、停用、作废和放行过程，包含操作人、操作时间、操作说明 | 发布记录完整 | QA |
| REQ-RULE-011 | MBR发布审批流程 | P0++ | MBR版本发布前必须经过工艺工程师→车间主管→QA三级审批 | 审批流程完整 | QA |

### 2.6 MBR变更管理

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-012 | MBR变更记录 | P0++ | 记录MBR变更来源（工艺优化/偏差纠正/ECN驱动/法规变更）、审批过程、影响范围和生效情况 | 变更记录完整 | 工艺工程师 |
| REQ-RULE-013 | MBR变更影响分析 | P0++ | 变更MBR时自动分析影响范围：哪些在执行中的工单受影响、哪些EBR需要刷新 | 影响分析准确 | 工艺工程师 |

### 2.7 工程变更通知（ECN）

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-014 | ECN单据管理 | P1++ | 管理正式ECN单据：ECN编号、来源（PLM/内部/法规）、变更类型（设计/工艺/物料/设备）、审批流程、生效策略（立即/指定日期/指定批次） | ECN单据信息完整 | 工艺工程师 |
| REQ-RULE-015 | ECN影响对象分析 | P1++ | 记录ECN影响的BOM、Routing、MBR、物料、设备、检验方案等对象 | 影响对象完整 | 工艺工程师 |
| REQ-RULE-016 | ECN与运行中工单联动 | P1++ | ECN影响运行中工单时，必须进入Safe Hold，刷新MBR执行上下文 | 联动逻辑正确 | 系统自动 |

### 2.8 MBR版本差异对比

| 需求ID | 需求名称 | 优先级 | 业务规则 | 验收标准 | 涉及角色 |
|--------|----------|--------|----------|----------|----------|
| REQ-RULE-017 | MBR版本差异对比 | P1++ | 记录MBR新旧版本在阶段、步骤、参数、物料、设备和质量项上的差异，支持可视化展示 | 差异展示清晰 | 工艺工程师 |

## 3. 核心流程

### 3.1 执行规则配置流程
1. 工艺工程师定义执行规则（防错/校验/放行/阻断）
2. 将规则关联到MBR步骤或全局
3. 配置规则优先级和触发条件
4. 提交审核，审核通过后生效

### 3.2 联锁触发处理流程
1. 执行过程中联锁条件被触发
2. 系统自动阻断执行或暂停
3. 记录联锁事件
4. 通知相关人员处理
5. 授权人员确认解除联锁
6. 恢复执行或转入异常流程

### 3.3 ECN处理流程
1. PLM或内部发起ECN
2. 工艺工程师分析影响范围
3. 审批ECN
4. 受影响的MBR进入Safe Hold
5. 刷新运行中工单的MBR执行上下文
6. 记录ECN处理全过程

## 4. 合规要求
1. 执行规则和联锁规则的变更必须经过审批
2. 联锁事件记录不可删除、不可篡改
3. 防错校验记录必须保留至少5年
4. ECN处理全过程必须可追溯
5. MBR版本差异对比必须支持审计检查

## 5. 与其他模块的关系
- L2 MBR模板 → L3 执行规则（规则关联到MBR步骤）
- L3 联锁规则 → L5 现场执行（联锁在执行时触发）
- L3 防错记录 → L6 异常管理（防错不通过触发异常）
- L3 ECN → L0 运行时韧性（ECN触发上下文刷新）
- L3 MBR发布 → L2 MBR版本（发布状态流转）
